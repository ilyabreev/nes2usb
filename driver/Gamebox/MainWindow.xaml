<Window x:Class="Gamebox.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Gamebox"
        mc:Ignorable="d"
        Title="MainWindow"
        WindowStartupLocation="CenterScreen" WindowState="Maximized"
        Loaded="Window_Loaded"
        KeyDown="Window_KeyDown"
        KeyUp="Window_KeyUp"
        Closed="Window_Closed">
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" Name="Scroll">
            <ListBox ItemsSource="{Binding Games}" SelectedIndex="0" HorizontalContentAlignment="Stretch" Name="GamesListBox">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Label FontSize="32" FontFamily="Verdana" Content="{Binding .}" HorizontalAlignment="Stretch"></Label>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>
    </Grid>
</Window>